import { ErrorHandlingInterceptor } from './error-handling-interceptor';

describe('ErrorHandlingInterceptor', () => {
  it('should create an instance', () => {
    expect(new ErrorHandlingInterceptor()).toBeTruthy();
  });
});
